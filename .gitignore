__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# run artefacts
*.log
outputs/

# data: large, and generated by process_blocks.py from the source block model
inputs/
*.pkl
*.csv
!requirements.txt

# schedules and reports emitted by run scripts
final_schedule.json
mip_report.json
beam_report.json
pushback_schedule*.json

# LaTeX build products (the .tex and .pdf in notes/ are kept)
notes/*.aux
notes/*.log
notes/*.out

# saved network weights
models/
